
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /avatars/{userId}/{fileName} {
      // Allow a user to read and write their own avatar
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
